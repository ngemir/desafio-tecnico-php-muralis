<?php
  // Comando para receber toda categoria
    // $resultado = $conexaobd->query('SELECT * FROM categorias');
    //   foreach($resultado as $cada){
    //     echo($cada['nome'] . ' = ' . $cada['descricao'] . '</br>');
    //   }


  // Lista do Tipo de Pagamento (Pronto)
  // echo "Result set order...\n";
  // foreach ($result as $row) {
  //     echo " tipo = " . $row['tipo'] . "\n";
  // }

  // SQL para inserção de despesas
  // INSERT INTO `despesas`(`valor`, `data_compra`, `descricao`, `tipo_pagamento_id`, `categoria_id`) VALUES ('300', STR_TO_DATE('15/02/2023', '%d/%m/%Y'),'Comprei celular do perigo','1','2')